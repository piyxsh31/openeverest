apiVersion: backup.openeverest.io/v1alpha1
kind: BackupStorage
metadata:
  labels:
    app.kubernetes.io/name: openeverest
    app.kubernetes.io/managed-by: kustomize
  name: backupstorage-sample
spec:
  # TODO(user): Add fields here
