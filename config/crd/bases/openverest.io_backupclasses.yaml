---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: backupclasses.openverest.io
spec:
  group: openverest.io
  names:
    kind: BackupClass
    listKind: BackupClassList
    plural: backupclasses
    singular: backupclass
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.displayName
      name: DisplayName
      type: string
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .spec.supportedProviders
      name: SupportedProviders
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BackupClass defines a reusable strategy for backing up data from
          a DataStore.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BackupClassSpec defines the specification of a BackupClass.
            properties:
              cleanupJobSpec:
                description: CleanupJobSpec is the specification of the cleanup job.
                properties:
                  command:
                    description: Command is the command to run the backup tool.
                    items:
                      type: string
                    type: array
                  image:
                    description: Image is the image of the backup tool.
                    type: string
                type: object
              clusterPermissions:
                description: |-
                  ClusterPermissions defines the cluster-wide permissions required by the backup tool.
                  These permissions are used to generate a ClusterRole for the backup job.
                items:
                  description: |-
                    PolicyRule holds information that describes a policy rule, but does not contain information
                    about who the rule applies to or which namespace the rule applies to.
                  properties:
                    apiGroups:
                      description: |-
                        APIGroups is the name of the APIGroup that contains the resources.  If multiple API groups are specified, any action requested against one of
                        the enumerated resources in any API group will be allowed. "" represents the core API group and "*" represents all API groups.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    nonResourceURLs:
                      description: |-
                        NonResourceURLs is a set of partial urls that a user should have access to.  *s are allowed, but only as the full, final step in the path
                        Since non-resource URLs are not namespaced, this field is only applicable for ClusterRoles referenced from a ClusterRoleBinding.
                        Rules can either apply to API resources (such as "pods" or "secrets") or non-resource URL paths (such as "/api"),  but not both.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    resourceNames:
                      description: ResourceNames is an optional white list of names
                        that the rule applies to.  An empty set means that everything
                        is allowed.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    resources:
                      description: Resources is a list of resources this rule applies
                        to. '*' represents all resources.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    verbs:
                      description: Verbs is a list of Verbs that apply to ALL the
                        ResourceKinds contained in this rule. '*' represents all verbs.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                  required:
                  - verbs
                  type: object
                type: array
              config:
                description: Config contains additional configuration defined for
                  the backup tool.
                properties:
                  openAPIV3Schema:
                    description: OpenAPIV3Schema is the OpenAPI v3 schema of the backup
                      tool.
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              dataStoreConstraints:
                description: |-
                  DataStoreConstraints defines compatibility requirements and prerequisites that must be satisfied
                  by a DataStore before this backup tool can be used with it. This allows the backup tool to
                  express specific requirements about the database configuration needed for successful backup operations,
                  such as required database fields, specific engine configurations, or other database properties.
                  When a DataStore references this backup tool, the operator will validate the DataStore
                  against these constraints before proceeding with the backup operation.
                properties:
                  requiredFields:
                    description: |-
                      RequiredFields contains a list of fields that must be set in the DataStore spec.
                      Each key is a JSON path expressions that points to a field in the DataStore spec.
                      For example, ".spec.engine.type" or ".spec.dataSource.dataImport.config.someField".
                    items:
                      type: string
                    type: array
                type: object
              description:
                description: Description is the description of the backup tool.
                type: string
              displayName:
                description: DisplayName is a human-readable name for the backup tool.
                type: string
              jobSpec:
                description: JobSpec is the specification of the backup job.
                properties:
                  command:
                    description: Command is the command to run the backup tool.
                    items:
                      type: string
                    type: array
                  image:
                    description: Image is the image of the backup tool.
                    type: string
                type: object
              permissions:
                description: |-
                  Permissions defines the permissions required by the backup tool.
                  These permissions are used to generate a Role for the backup job.
                items:
                  description: |-
                    PolicyRule holds information that describes a policy rule, but does not contain information
                    about who the rule applies to or which namespace the rule applies to.
                  properties:
                    apiGroups:
                      description: |-
                        APIGroups is the name of the APIGroup that contains the resources.  If multiple API groups are specified, any action requested against one of
                        the enumerated resources in any API group will be allowed. "" represents the core API group and "*" represents all API groups.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    nonResourceURLs:
                      description: |-
                        NonResourceURLs is a set of partial urls that a user should have access to.  *s are allowed, but only as the full, final step in the path
                        Since non-resource URLs are not namespaced, this field is only applicable for ClusterRoles referenced from a ClusterRoleBinding.
                        Rules can either apply to API resources (such as "pods" or "secrets") or non-resource URL paths (such as "/api"),  but not both.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    resourceNames:
                      description: ResourceNames is an optional white list of names
                        that the rule applies to.  An empty set means that everything
                        is allowed.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    resources:
                      description: Resources is a list of resources this rule applies
                        to. '*' represents all resources.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    verbs:
                      description: Verbs is a list of Verbs that apply to ALL the
                        ResourceKinds contained in this rule. '*' represents all verbs.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                  required:
                  - verbs
                  type: object
                type: array
              supportedProviders:
                description: SupportedProviders is the list of providers that the
                  backup tool supports.
                items:
                  type: string
                type: array
            type: object
          status:
            description: BackupClassStatus defines the status of the BackupClass.
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
