describe('Database form default values', () => {
  it('For select field with optionsPath in the schema the value should be getted from Provider object', () => {
    // TODO maybe move to ui-generator select tests
  });
});
