describe('Database form navigation', () => {
  it('Configure more options btn should navigate to the next step', () => {
    // TODO
  });
  it.skip('Configure more options btn should navigate to the next step and shows tour tooltips to hilight abilities of editing and navigation on the form', () => {
    // TODO recheck after v2 release
  });
  it('Cancel should cause cancelling modal', () => {});
  it('From second section to sections.length-1 use can see previous, cancel, continue steps', () => {});
  it('Create database button should be shown in the last step', () => {});
  it('Create database button should be shown in the first step', () => {});
});
